import 'package:flutter/material.dart';

class DebtorsPage extends StatelessWidget {
  const DebtorsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("this is the debtors page"));
  }
}
